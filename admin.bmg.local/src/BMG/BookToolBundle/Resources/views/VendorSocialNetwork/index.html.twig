{% extends '::base.html.twig' %}

{% block body -%}
    <h1>VendorSocialNetwork list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Vendorsocialnetworkaccount</th>
                <th>Vendorsocialnetworkid</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('vendorsocialnetwork_show', { 'id': entity.vendorSocialNetworkId }) }}">{{ entity.vendorSocialNetworkAccount }}</a></td>
                <td>{{ entity.vendorSocialNetworkId }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('vendorsocialnetwork_show', { 'id': entity.vendorSocialNetworkId }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('vendorsocialnetwork_edit', { 'id': entity.vendorSocialNetworkId }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('vendorsocialnetwork_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
