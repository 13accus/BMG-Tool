{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ProjectVendorClientLink list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Projectvendorclientlinkdatetime</th>
                <th>Projectvendorclientlinkid</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('projectvendorclientlink_show', { 'id': entity.projectVendorClientLinkId }) }}">{{ entity.projectVendorClientLinkDatetime }}</a></td>
                <td>{{ entity.projectVendorClientLinkId }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('projectvendorclientlink_show', { 'id': entity.projectVendorClientLinkId }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('projectvendorclientlink_edit', { 'id': entity.projectVendorClientLinkId }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('projectvendorclientlink_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
