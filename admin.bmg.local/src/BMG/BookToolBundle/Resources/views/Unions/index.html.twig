{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Unions list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Unionname</th>
                <th>Unionid</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('unions_show', { 'id': entity.Unionid }) }}">{{ entity.unionName }}</a></td>
                <td>{{ entity.unionId }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('unions_show', { 'id': entity.Unionid }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('unions_edit', { 'id': entity.Unionid }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('unions_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
