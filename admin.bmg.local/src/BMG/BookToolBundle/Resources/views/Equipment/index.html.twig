{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Equipment list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Equipmentname</th>
                <th>Equipmentid</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('equipment_show', { 'id': entity.equipmentId }) }}">{{ entity.equipmentName }}</a></td>
                <td>{{ entity.equipmentId }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('equipment_show', { 'id': entity.equipmentId }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('equipment_edit', { 'id': entity.equipmentId }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('equipment_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
