{% extends '::base.html.twig' %}

{% block body -%}
    <h1>HearAbout list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Hearaboutname</th>
                <th>Hearaboutid</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('hearabout_show', { 'id': entity.hearAboutId }) }}">{{ entity.hearAboutName }}</a></td>
                <td>{{ entity.hearAboutId }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('hearabout_show', { 'id': entity.hearAboutId }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('hearabout_edit', { 'id': entity.hearAboutId }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('hearabout_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
