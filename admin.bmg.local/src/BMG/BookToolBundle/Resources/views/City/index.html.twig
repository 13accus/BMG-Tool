{% extends '::base.html.twig' %}

{% block body -%}
    <h1>City list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Cityname</th>
                <th>Cityzipcode</th>
                <th>Citylatitude</th>
                <th>Citylongitude</th>
                <th>Citycounty</th>
                <th>Cityid</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('city_show', { 'id': entity.cityId }) }}">{{ entity.cityName }}</a></td>
                <td>{{ entity.cityZipcode }}</td>
                <td>{{ entity.cityLatitude }}</td>
                <td>{{ entity.cityLongitude }}</td>
                <td>{{ entity.cityCounty }}</td>
                <td>{{ entity.cityId }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('city_show', { 'id': entity.cityId }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('city_edit', { 'id': entity.cityId }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('city_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
