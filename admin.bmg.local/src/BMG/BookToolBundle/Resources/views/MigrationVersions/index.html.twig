{% extends '::base.html.twig' %}

{% block body -%}
    <h1>MigrationVersions list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Version</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('migrationversions_show', { 'id': entity.version }) }}">{{ entity.version }}</a></td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('migrationversions_show', { 'id': entity.version }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('migrationversions_edit', { 'id': entity.version }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('migrationversions_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
